/**
 * Build PWA pública: API relativa vía proxy (:8300 / ngrok).
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
  defaultLat: 4.5255,
  defaultLng: -76.0755,
  defaultRadiusKm: 50,
  fallbackRadiusKm: 150,
  appName: 'Hidrix',
};
